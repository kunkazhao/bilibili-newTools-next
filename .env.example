# Frontend API base URL.
# Local dev can keep this empty because Vite proxy forwards /api to 127.0.0.1:8000.
# Set this when your frontend and backend are on different hosts.
VITE_API_BASE=http://127.0.0.1:8000
